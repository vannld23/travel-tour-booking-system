<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd
http://www.springframework.org/schema/mvc
http://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <!-- Scan Java classes with annotations such as @Controller -->
    <context:component-scan base-package="uef.edu.vn"/>
    <!--Enable the use of annotations such as @RequestMapping -->
    <mvc:annotation-driven/>    
    
    <mvc:interceptors>
        <mvc:interceptor>
            <!-- Protect all admin endpoints -->
            <mvc:mapping path="/dashboard"/>
            <mvc:mapping path="/dashboard/**"/>
            <mvc:mapping path="/admin/**"/>
            <mvc:mapping path="/booking"/>
            <mvc:mapping path="/booking/**"/>
            <mvc:mapping path="/tuormanagement"/>
            <mvc:mapping path="/tuormanagement/**"/>
            <mvc:mapping path="/destination"/>
            <mvc:mapping path="/destination/**"/>
            <mvc:mapping path="/itinerary"/>
            <mvc:mapping path="/itinerary/**"/>
            <mvc:mapping path="/payment"/>
            <mvc:mapping path="/payment/**"/>
            <mvc:mapping path="/system"/>
            <mvc:mapping path="/system/**"/>
            
            <!-- Exclude client endpoints -->
            <mvc:exclude-mapping path="/booking/create"/>
            <mvc:exclude-mapping path="/booking/history/**"/>
            <mvc:exclude-mapping path="/payment/history/**"/>
            
            <bean class="uef.edu.vn.interceptor.RoleInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <mvc:resources mapping="/resources/**"
               location="/resources/" />
    <mvc:resources mapping="/resource/**"
               location="/resource/" />
    <!-- Configure the view resolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- MultipartResolver: Ho tro upload file/anh -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"/> <!-- 10MB -->
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

</beans>